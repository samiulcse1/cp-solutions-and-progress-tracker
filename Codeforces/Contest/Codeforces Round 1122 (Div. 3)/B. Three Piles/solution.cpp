#include <bits/stdc++.h>
using namespace std;

int main()
{

    int t;
    cin >> t;

    while (t--)
    {
        vector<long long> a(3);
        for (int i = 0; i < 3; i++)
        {
            cin >> a[i];
        }
    }

    return 0;
}
